<%= form_with(model: audition_request, class: "contents") do |form| %>
  <% if audition_request.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-md mt-3">
      <h2><%= pluralize(audition_request.errors.count, "error") %> prohibited this audition_request from being saved:</h2>

      <ul class="list-disc ml-6">
        <% audition_request.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="my-5">
    <%= form.label :call_to_audition_id %>
    <%= form.collection_select :call_to_audition_id, CallToAudition.all, :id, :production_name, prompt: "Select a Call to Audition" %>
  </div>

  <div class="my-5">
    <%= form.label :person_id %>
    <%= form.collection_select :person_id, Person.all, :id, :stage_name, prompt: "Select a Person" %>
   </div>

  <div class="inline">
    <%= form.submit class: "w-full sm:w-auto rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
