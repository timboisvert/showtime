<nav class="fixed top-0 left-0 right-0 w-screen bg-white shadow z-50">
    <div class="max-w-full flex items-center justify-between">
    <div class="flex items-center space-x-2">
        <img src="<%= asset_path('cocoscoutsmall.png') %>" alt="CocoScout" class="select-none w-10 ml-2 mt-2 mr-4" />
        <span class="font-bold text-3xl pt-2 coustard-regular">CocoScout</span>
    </div>
    <div class="relative">
        <button id="prod-menu-btn" class="flex items-center space-x-2 focus:outline-none">
        <span class="flex items-center justify-center h-10 w-10 rounded-full bg-purple-200 text-purple-700 font-bold text-lg">
        <%#= Current.user.production_company.initials %>
        TB
        </span>
        <span class="font-medium text-slate-700">Coco Runs Everything</span>
        <svg class="w-4 h-4 ml-1 text-slate-500" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7"/>
        </svg>
        </button>
        <!-- Dropdown menu placeholder -->
        <!-- <div class="hidden absolute right-0 mt-2 w-48 bg-white rounded shadow-lg z-50"> ... </div> -->
    </div>
    </div>
</nav>